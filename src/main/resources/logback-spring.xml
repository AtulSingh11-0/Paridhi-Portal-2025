<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with thread information -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%blue(%X{requestId})] [%magenta(%thread)] %yellow(%C{1}) - %msg%n%throwable</pattern>
        </encoder>
    </appender>
    
    <!-- Async wrapper for console appender for non-blocking performance -->
    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="Console" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- Production configuration -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE" />
        </root>
        
        <!-- Application-specific logs -->
        <logger name="com.megatronix.paridhi" level="INFO" additivity="false">
            <appender-ref ref="ASYNC_CONSOLE" />
        </logger>
        
        <!-- Reduce noise from common libraries -->
        <logger name="org.apache.catalina" level="WARN" />
        <logger name="org.springframework" level="WARN" />
        <logger name="org.hibernate" level="WARN" />
        <logger name="com.zaxxer.hikari" level="WARN" />
    </springProfile>

    <!-- Development configuration -->
    <springProfile name="test">
        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE" />
        </root>
        
        <!-- More detailed application logs for development -->
        <logger name="com.megatronix.paridhi" level="DEBUG" additivity="false">
            <appender-ref ref="ASYNC_CONSOLE" />
        </logger>
    </springProfile>

    <!-- Development configuration -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE" />
        </root>
        
        <!-- More detailed application logs for development -->
        <logger name="com.megatronix.paridhi" level="DEBUG" additivity="false">
            <appender-ref ref="ASYNC_CONSOLE" />
        </logger>
    </springProfile>
</configuration>